name: Observe Changes in Web Page.
on:
  push:
    branches:
      - 'main'
jobs:
  observe_file_changes:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v3

      - name: Get JSON file data
        run: echo "The JSON file data is: $(cat ./src/properties/homeComponent.json)"

      - name: Send Webhook
        run: curl -X POST -H "Content-Type: application/json" -d "$JSON_FILE_DATA" https://rowy-hooks-fdiwibo4ia-wn.a.run.app/wh/properties/gXO9N8ebsKTrbZt3JFgk